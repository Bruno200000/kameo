{
  "name": "kameo",
  "version": "1.0.0",
  "description": "Kameo SaaS - Full Stack",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install-all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
